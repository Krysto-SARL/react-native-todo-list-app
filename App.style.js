import { StyleSheet } from 'react-native'

const s = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    padding: 20,
  },
  text: {
    color: 'blue',
  },
})

export { s }
